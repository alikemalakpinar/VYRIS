# ─── Database ──────────────────────────────────────────────
DATABASE_URL=postgresql://vyris:vyris_local@localhost:5432/vyris

# ─── Redis ─────────────────────────────────────────────────
REDIS_URL=redis://127.0.0.1:6379

# ─── Server ────────────────────────────────────────────────
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# ─── Drop Configuration ───────────────────────────────────
DROP_TIER=genesis
DROP_YEAR=2025
DROP_CAPACITY=999

# ─── JWT / Encounter Verification (ES256 public key PEM) ──
# Generate a key pair:
#   openssl ecparam -genkey -name prime256v1 -noout -out ec_private.pem
#   openssl ec -in ec_private.pem -pubout -out ec_public.pem
# Then paste the public key contents here (replace newlines with \n):
ENCOUNTER_JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nMFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEexamplekeyhere\n-----END PUBLIC KEY-----"

# ─── PKPass Signing (stubs — fill for production) ─────────
PKPASS_CERTIFICATE=
PKPASS_KEY=
PKPASS_PASSPHRASE=

# ─── Reforge / Email ──────────────────────────────────────
REFORGE_FROM_EMAIL=noreply@vyris.app
REFORGE_BASE_URL=https://vyris.app

# ─── Apple Receipt ────────────────────────────────────────
APPLE_SHARED_SECRET=
